# Scalable-Categorization-of-satellite-images
A Django web app that uses a deep learning model to automatically categorize satellite images. It supports dataset upload, model training, and real-time image classification with optimized accuracy and scalability.
